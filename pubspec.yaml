name: structure_radar
description: Multi-exchange market structure scanner — trend shifts, MA regime, S/R levels.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  http: ^1.4.0
  intl: ^0.20.2
  provider: ^6.1.5
  shared_preferences: ^2.5.5
  uuid: ^4.5.1
  url_launcher: ^6.3.1
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
